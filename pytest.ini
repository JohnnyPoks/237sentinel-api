[pytest]
# Unit tests never download model weights. Integration tests that hit real
# models are marked "slow" and excluded from CI (run with: pytest -m slow).
markers =
    slow: hits real Hugging Face models; excluded from CI
addopts = -q -m "not slow"
testpaths = tests
