# --- Web framework ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
pydantic==2.10.4
pydantic-settings==2.7.1

# --- Database ---
sqlalchemy==2.0.36
alembic==1.14.0
psycopg[binary]==3.2.3

# --- HTTP / links ---
httpx==0.28.1
tldextract==5.1.3
python-whois==0.9.5

# --- ML / inference (CPU) ---
# torch is installed via the Dockerfile with the CPU index URL to avoid CUDA.
transformers==4.48.0
torch==2.5.1
onnxruntime==1.20.1

# --- Media / forensics ---
pillow==11.1.0
opencv-python-headless==4.11.0.86
numpy==2.2.1
pymupdf==1.25.2
easyocr==1.7.2
faster-whisper==1.1.1

# --- Optional LLM providers (import-guarded; not required to boot) ---
anthropic==0.42.0

# --- Telegram bot (optional) ---
python-telegram-bot==21.9

# --- Testing ---
pytest==8.3.4
pytest-asyncio==0.25.0
